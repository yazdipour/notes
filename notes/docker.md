# Docker

```shell
	docker ps //to see running image
	docker stop (dockerID_from_ps) //STOP
	docker build -t x/y ./dir  // Build Image in x/y Repository
	docker images  // list of all available  images
	docker run -p 80:5000 x/y
```